dependencies {
    api project(':provider-api')

    annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'

    implementation "com.google.apis:google-api-services-oauth2:${googleApiServicesOAuth2Version}"
}
